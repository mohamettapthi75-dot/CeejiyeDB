[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "ceejiyedb"
version = "0.1.0"
description = "Somali-syntax database engine with Rust core"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.maturin]
module-name = "ceejiye_core"
manifest-path = "core/Cargo.toml"
